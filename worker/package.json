{
  "name": "deniz-chat-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that powers the 'Ask me about my past projects' chatbot on denizjafari.com",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
